# Python bytecode
__pycache__/
*.py[cod]

# Virtual environments
.venv/
env/
venv/
ENV/

# Jupyter
.ipynb_checkpoints/
# Ignore notebook checkpoint outputs, but keep notebooks themselves
# Optionally ignore output files generated by nbconvert
*.nbconvert.ipynb

# Data: avoid committing raw or staging data
data/raw/
data/staging/

# If you want to track curated datasets, do NOT ignore data/curated/
# To keep an empty folder tracked, add data/curated/.gitkeep

# OS and editor files
.DS_Store
Thumbs.db
.vscode/
.idea/

# Secrets and environment config
.env
*.key
credentials.json
secrets/
*.pem

# Testing and coverage
.pytest_cache/
.coverage
coverage.xml

# Build / packaging
dist/
build/
*.egg-info/

# Cache / tooling
.mypy_cache/
.cache/
.ipynb_checkpoints/

# Git LFS hint for large data files (uncomment and configure if using LFS)
# *.parquet filter=lfs diff=lfs merge=lfs -text
# *.feather filter=lfs diff=lfs merge=lfs -text
